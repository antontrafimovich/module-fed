import { createRoute } from 'atomic-router';

export const homeRoute = createRoute();
export const assessmentRoute = createRoute();
export const assessmentDetailsRoute = createRoute();
export const notFoundRoute = createRoute();
